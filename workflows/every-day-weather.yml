name: Send a telegram message abount today's weather every morning

on:
  weather:
    apiKey: ${{ secrets.OPENWEATHERMAP_API_KEY }}
    params:
      lat: 59.8944
      lon: 30.2642
      units: metric
    every: 0
    timeZone: UTC+3

jobs:
  notify:
    name: Send notifications
    runs-on: ubuntu-latest
    steps:
      - name: View context attributes
        uses: actions/github-script@v4
        with:
          script: |
            console.log(context)

      - name: Prepare a telegram message
        uses: actions/github-script@v4
        id: message
        env:
          WEATHER_DATA: ${{ toJSON(on.weather.outputs) }}
        with:
          script: |
            /**
             * Process env variables
             * @type {string[]}
             */
            const {WEATHER_DATA} = process.env

            /**
             * Side directions
             * @type {string[]}
             */
            const DIRECTIONS = ['north', 'north-west', 'west', 'south-west', 'south', 'south-east', 'east', 'north-east'];

            /**
             * Locale
             * @type {string}
             */
            const LOCALE = 'en-GB';

            /**
             * Date format
             * @type {{hour: string, timeZone: string, minute: string}}
             */
            const DATE_FORMAT = {
                hour: '2-digit',
                minute: '2-digit',
                timeZone: 'Europe/Moscow',
            };

            /**
            * Returns formatted side direction by input angle in degrees
            * @param angle initial input angle in degrees to calculate by
            * @returns {string} formatted side direction
            */
            const formatSideDirection = value => {
                return config.DIRECTIONS[Math.round(((value %= 360) < 0 ? value + 360 : value) / 45) % 8];
            };

            /**
            * Returns formatted date by input date/time pattern
            * @param value initial input date to format
            * @returns {string} short formatted date
            */
            const formatDateByPattern = value => {
              return new Date(value * 1000).toLocaleString(config.LOCALE, config.DATE_FORMAT);
            };

            console.log(JSON.stringify(WEATHER_DATA));

            const longitude = WEATHER_DATA.lon;
            const latitude = WEATHER_DATA.lat;
            const timezone = WEATHER_DATA.timezone;

            console.log(${{ on.weather.outputs.current.weather[0].description }});
            const main = WEATHER_DATA.current.weather[0].main;
            const description = WEATHER_DATA.current.weather[0].description;

            const tempMin = WEATHER_DATA.daily[0].temp.min;
            const tempMax = WEATHER_DATA.daily[0].temp.max;

            const temperature = Math.round(WEATHER_DATA.current.temp);
            const temperatureLike = Math.round(WEATHER_DATA.current.feels_like);
            const pressure = Math.round(WEATHER_DATA.current.pressure);
            const humidity = Math.round(WEATHER_DATA.current.humidity);
            const visibility = Math.round(WEATHER_DATA.current.visibility);
            const clouds = Math.round(WEATHER_DATA.current.clouds);
            const uvi = Math.round(WEATHER_DATA.current.uvi);

            const wind = Math.round(WEATHER_DATA.current.wind_speed);
            const windDirection = formatSideDirection(WEATHER_DATA.current.wind_deg);

            const sunrise = formatDateByPattern(WEATHER_DATA.current.sunrise);
            const sunset = formatDateByPattern(WEATHER_DATA.current.sunset);

            return `
                Currently, the weather in <= Saint-Petersburg => (lon: ${longitude}, lat: ${latitude}, timezone: ${timezone}) is:

                temperature: ${temperature}°C [${tempMin}-${tempMax}°C] (${main}, ${description}), fells like ${temperatureLike}°C,

                humidity: ${humidity}%,
                pressure: ${pressure} (mbar),
                visibility: ${visibility},
                uvi: ${uvi},

                wind: /${windDirection}/ ${wind} (m/s),
                cloudy: ${clouds}%,

                today, the Sun rises at ${sunrise}am and sets at ${sunset}pm.
            `;
          result-encoding: string

      - name: Send a telegram message
        uses: appleboy/telegram-action@v0.1.0
        with:
          to: ${{ secrets.TELEGRAM_TO }}
          token: ${{ secrets.TELEGRAM_TOKEN }}
          message: ${{ steps.message.outputs.result }}
